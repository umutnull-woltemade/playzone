{
  "name": "playzone",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "nuxt build && wrangler pages deploy dist --project-name=playzone",
    "deploy:preview": "nuxt build && wrangler pages deploy dist --project-name=playzone --branch=preview"
  },
  "dependencies": {
    "@nuxtjs/sitemap": "^7.5.0",
    "@vueuse/core": "^14.1.0",
    "@vueuse/motion": "^3.0.3",
    "nuxt": "^3.14.0",
    "vue": "^3.5.0",
    "vue-router": "^4.4.0"
  },
  "devDependencies": {
    "@nuxt/icon": "^1.5.0",
    "@nuxtjs/tailwindcss": "^6.12.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
